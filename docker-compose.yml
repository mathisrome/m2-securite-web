services:
  mariadb:
    image: mariadb:11.1.2
    restart: unless-stopped
    volumes:
      - ./infra/volumes/dev/database:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"

  with-security-problem-symfony-php:
    build:
      context: ./infra/dev/with-security-problem/symfony-php
    volumes:
      - ./infra/volumes/dev/composer:/root/.composer
      - ./infra/dev/with-security-problem/symfony-php/project.ini:/usr/local/etc/php/conf.d/project.ini:ro
      - ./with-security-problem:/var/www/app
    environment:
      APP_ENV: dev
    depends_on:
      - mariadb
    tty: true

  with-security-problem-symfony-nginx:
    image: nginx:latest
    volumes:
      - ./infra/volumes/dev/logs/nginx:/var/log/nginx
      - ./infra/dev/with-security-problem/symfony-nginx/virtualhost.conf:/etc/nginx/conf.d/default.conf:ro
      - ./with-security-problem:/var/www/app
    ports:
      - "80:80"
    depends_on:
      - with-security-problem-symfony-php
    tty: true

  without-security-problem-symfony-php:
    build:
      context: ./infra/dev/without-security-problem/symfony-php
    volumes:
      - ./infra/volumes/dev/composer:/root/.composer
      - ./infra/dev/without-security-problem/symfony-php/project.ini:/usr/local/etc/php/conf.d/project.ini:ro
      - ./without-security-problem:/var/www/app
    environment:
      APP_ENV: dev
    depends_on:
      - mariadb
    tty: true

  without-security-problem-symfony-nginx:
    image: nginx:latest
    volumes:
      - ./infra/volumes/dev/logs/nginx:/var/log/nginx
      - ./infra/dev/without-security-problem/symfony-nginx/virtualhost.conf:/etc/nginx/conf.d/default.conf:ro
      - ./without-security-problem:/var/www/app
    ports:
      - "81:80"
    depends_on:
      - without-security-problem-symfony-php
    tty: true

  mailcatcher:
    restart: on-failure
    image: schickling/mailcatcher
    ports: [ 1025, 1080:1080 ]
